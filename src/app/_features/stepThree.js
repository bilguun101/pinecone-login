'use client'
import { useState } from "react";
import { FormInput } from "../_components/form-input";

export const StepThree = () => {
    return <div> Now this is a text that you will see if the third step works. If not, then I'm giving up on life. </div>
}